

class Main {

	string name; 
    
    void init() {
        name = "gcd";
    }
	
	/* computes gcd of a and b (positive integers) */
	int gcd(int a, int b) {
		while (b > 0) { 
			c = a % b;  
			a = b; b = c; 
		}
		return a;
	}
	
}
